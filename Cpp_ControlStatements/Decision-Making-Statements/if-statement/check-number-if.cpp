#include<iostream>
using namespace std;
main()
{
	int num;
	cout<<"Enter number : ";
	cin>>num;
	if(num>0)
	{
		cout<<"Number is positive";
	}
	if(num<0)
	{
		cout<<"Number is negative";
	}
	if(num==0)
	{
		cout<<"Number is zero";
	}
}
